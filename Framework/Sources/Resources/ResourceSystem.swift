import SedulousJobs

public class ResourceSystem
{
    private let jobSystem: JobSystem

    package init(jobSystem: JobSystem) {
        self.jobSystem = jobSystem
    }

    package func startup() {
    }

    package func shutdown() {
    }

    package func update() {
    }
}